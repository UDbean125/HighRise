# Generated Xcode project (regenerate with `xcodegen generate`)
*.xcodeproj

# Master icon artwork (large source files kept on the user's drive, not in git;
# this folder lives inside the working copy on that Mac).
HighRise iCons/

# Xcode / build artifacts
build/
DerivedData/
*.xcuserstate
xcuserdata/
.DS_Store
Google ID's/
